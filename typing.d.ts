type Message = {
    content: string;
    senderId?: string;
    sender: boolean;
    // timestamp?: Date;
    // status?: 'sent' | 'received';
    // isRead?: boolean;
    // isReply?: boolean;
    // replyTo?: MessageProps;
    // children?: React.ReactNode;
    // className?: string;
}